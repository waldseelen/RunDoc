rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{projectId}/{allPaths=**} {
      // Protect user files: require authentication for read & write operations
      allow read, write: if request.auth != null;
    }
  }
}
